vamos a programar un proxy socks v5

que atienda a multiples clientes

autenticacionen usuario/contraseña

ipv4 o ipv6

conectarnos bien al dns como se debería probando todas

reportar los fallos al cliente (404 etc)

acumular métricas. si se apaga el servidor se reinician, son variables globales y listo.
(otro puerto para accederlos)

un poco de settings variables (usando ese mismo otro puerto).

registro de acceso (acces_log.log)

monitorear el tráfico y chorear pass de pop3.



requerimientos:
-C, variante C11. (cambian un poqutio cosas de estructura? están mejores).
-Utilizando sockets no bloqueantes multiplexados (usando función select).
-Buen test: navegar usando el proxy, y tiene que andar, y no mal ni lento.
-Elegir bien tamaños de buffers y eso. Pensando en las buenas prácticas.
-Informe cheto (mas info en el enunciado, responder unas preguntas). Ir armandolo en tiempo real, no escribir todo al final.
-Hacer el RFC
-Seguir unos lineamientos que nos dan ellos. (flags, nombres de archivos etc.)
-Uso de librerías: (sería raro usar una, alguna de logging talvez)
	no debe resolver cuestiones del fondo del tp
	debe ser open source
	hay que pedir permiso a la cátedra.
-manejar bien la ambiguedad e incertidumbre
	dividir los problemas y decidir cual batalla pelear

gcc clang en pampero debería estar masomenos bien para compilar.
la idea es que no tenga warnings ni en gcc ni en clang


Evaluación
grupos de hasta 4.
hay coloquio en la entrega.
se entrega el repo git con el historial.
Informe con el contenido en el enunciado
codigo fuente
archivos de construcción
readme (indice)

se entrega en campus

condiciones necesarias para aprobar:
el material debe estar completo
solo usar librerías permitidas
correcta las cuestiones de entradas/salidas no bloqueante.
correcta las cuestiones de lectura/escritura parcial. (tipo que no llegue todo del tcp stream etc.)
llegar al 4

Entrega tardía es 24hs más. Tiene nota máxima un 4.

Ellos nos dan el man, y tenemos que seguir sus lineamientos e implementar todo.
man ./nombre_archivo

los registros son por salida estándar.
´
